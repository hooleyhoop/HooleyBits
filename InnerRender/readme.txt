Construct a path, get an image
------------------------------

steps,

1) given a path that fits into a large image, render into a large image
2) render into the smallest possible image

problems,

1) paths have different formats, can be open or closed, etc
2) images can be 1 bit, 8 bit, etc
